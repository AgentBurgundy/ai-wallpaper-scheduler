name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
    
    - name: Install dependencies
      run: yarn install
    
    - name: Build Electron App
      run: yarn build:electron
    
    - name: Upload Release Assets
      uses: actions/upload-artifact@v4
      with:
        name: screensaver-windows-${{ github.ref_name }}
        path: dist-installer/*.exe
    
    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          dist-installer/*.exe
        body: |
          ## AI Wallpaper Generator ${{ github.ref_name }}
          
          ### 🚀 Quick Start
          
          1. Download and run the installer (`Screensaver-Setup-*.exe`)
          2. The app will start in your system tray (look for the icon near your clock)
          3. Right-click the tray icon → **Open Settings**
          4. Enter your Google API key and configure your preferences
          5. Click **Generate Wallpaper** to create your first AI wallpaper!
          
          ### ✨ Features
          
          - 🎨 AI-powered wallpaper generation using Google Gemini
          - 📌 Lives in your system tray - always accessible
          - ⚙️ Easy settings UI - no config files to edit
          - 📅 Automatic daily wallpaper updates
          - 🔄 Auto-updates when new versions are released
          - 🖼️ Supports 1K, 2K, and 4K resolutions
          
          ### 📋 Requirements
          
          - Windows 10/11
          - Google API key (free from [Google AI Studio](https://aistudio.google.com/apikey))
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

